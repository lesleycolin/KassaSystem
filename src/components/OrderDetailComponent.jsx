import { StyleSheet, Text, View } from "react-native";
import React from "react";

const OrderDetailComponent = () => {
  return (
    <View>
      <Text>OrderDetailComponent</Text>
    </View>
  );
};

export default OrderDetailComponent;

const styles = StyleSheet.create({});
