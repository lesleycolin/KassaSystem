import { StyleSheet, Text, View } from "react-native";
import React from "react";

const OrderOverviewComponent = () => {
  return (
    <View>
      <Text>OrderOverviewComponent</Text>
    </View>
  );
};

export default OrderOverviewComponent;

const styles = StyleSheet.create({});
